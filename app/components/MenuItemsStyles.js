import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({

	menuItem: {
		width: '33.33333%',
		height: '50%',
		padding: 20,
	},

	image: {
		width: '100%',
		height: '100%',
		opacity: 0.8,
		borderColor: '#fff',
		borderWidth: 3
	}

});

export default Styles;